server {
    listen 80;
    server_name localhost;

    root  /usr/share/nginx/html;
    index index.html;

    # Serve all static files directly
    # Since we call the backend via direct localhost:8000 from the browser,
    # no proxy configuration is needed here
    location / {
        try_files $uri $uri/ /index.html;
    }
}
